"""Aegis brand palette for CLI rendering.

Single source for the colors used by aegis CLI surfaces (the guided setup
today; other commands as they adopt the brand styling). The hex values
mirror the generated frontend theme
(``app/components/frontend/theme.py`` in the template) so the CLI and the
product it generates share one visual identity.
"""

AEGIS_TEAL = "#17CCBF"
"""Primary teal/cyan — the brand standard accent."""

AEGIS_TEAL_DARK = "#248F87"
"""Darker teal — secondary accent."""
